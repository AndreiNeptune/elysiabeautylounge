# Elysia Beauty Lounge — Environment Variables Template
# Copy this file to .env.local to configure environment variables.
# Vite env variables must be prefixed with VITE_ to be exposed to client-side code.

# Example:
# VITE_API_URL=https://api.elysiabeautylounge.ro
